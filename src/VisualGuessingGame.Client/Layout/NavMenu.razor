<div class="top-row ps-3 navbar navbar-dark">
    <div class="container-fluid">
        <button title="Navigation menu" class="navbar-toggler" @onclick="ToggleNavMenu">
            <span class="navbar-toggler-icon"></span>
        </button>
        <a class="navbar-brand" href="generate">Configuration</a>
    </div>
</div>

<div class="@NavMenuCssClass nav-scrollable" @onclick="ToggleNavMenu">
    <nav class="flex-column">
        <div class="nav-item px-3">
            <NavLink class="nav-link" href="generate" Match="NavLinkMatch.All">
                <span class="bi bi-house-door-fill-nav-menu" aria-hidden="true"></span> Generate images
            </NavLink>
        </div>
        <div class="nav-item px-3">
            <NavLink class="nav-link" href="prepare">
                <span class="bi bi-plus-square-fill-nav-menu" aria-hidden="true"></span> Prepare game
            </NavLink>
        </div>
        <div class="nav-item px-3">
            <NavLink class="nav-link" href="testgame">
                <span class="bi bi-list-nested-nav-menu" aria-hidden="true"></span> Test game
            </NavLink>
        </div>
    </nav>
</div>

@code {
    private bool collapseNavMenu = false;

    private string? NavMenuCssClass => collapseNavMenu ? "collapse" : null;

    private void ToggleNavMenu()
    {
        collapseNavMenu = !collapseNavMenu;
    }

}